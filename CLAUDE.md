# CLAUDE.md

This file provides guidance to Claude Code (claude.ai/code) when working with code in this repository.

## Overview

This is a C++ project repository. No source files have been added yet — this file should be updated once the project structure is established.

## Getting Started

As files are added, update this file with:
- Build commands (e.g., `cmake`, `make`, `g++` invocations)
- Test commands and how to run a single test
- Any linting or formatting tools in use (e.g., `clang-format`, `clang-tidy`)
- High-level architecture once the codebase has meaningful structure
